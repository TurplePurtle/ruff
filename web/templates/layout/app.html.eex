<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Ruff!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav class="top-nav" role="navigation">
          <%= if @current_user do %>
            <%= link @current_user.username, to: "/", class: "nav-link" %>
            <%= button "Log out", to: session_path(@conn, :delete), method: :delete, class: "nav-link" %>
          <% else %>
            <%= link "Sign up", to: registration_path(@conn, :new), class: "nav-link" %>
            <%= link "Log in", to: session_path(@conn, :new), class: "nav-link" %>
          <% end %>
        </nav>
      </header>

      <div class="row">
        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      </div>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
